server:
  port: ${PORT:8081}

spring:
  application:
    name: money-tracker-api
    api-key: ${API_KEY:}
    cors:
      allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-d8dtegernols739n4re0-a.singapore-postgres.render.com:5432/db_money_tracker}
    username: ${DB_USERNAME:dongadmin}
    password: ${DB_PASSWORD:}


#    url: jdbc:h2:mem:testdb
#      driver-class-name: org.h2.Driver
#      username: sa
#      password:
#
#  h2:
#    console:
#      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        dialect: org.hibernate.dialect.H2Dialect

logging:
  level:
    org.hibernate.SQL: DEBUG